describe('SSMLoader.load', () => {
  describe('If SSM library cannot be located', () => {
    it('Will throw', () => {
      // expect(true).toBe(false);
    });
  });

  // TODO:
  // will throw & log if getParameter fails
  // decryption settings
  // will return parameter value
});